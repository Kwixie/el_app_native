import { SafeAreaView, StyleSheet, Text, View } from "react-native";
import React from "react";

const Kalkylator = () => {
  return (
    <SafeAreaView>
      <View>
        <Text>Kalkylator</Text>
      </View>
    </SafeAreaView>
  );
};

export default Kalkylator;

const styles = StyleSheet.create({});
